{% extends 'templates/app.twig' %}

{% block content %}

    <div class="row">

    {% for index, product in products %}
        <div class="col-md-4 col-sm-6">
Product
        </div>

        {% if (index +1) % 3 == 0 %}
            </div>
            <div class="row"></div>
        {% endif %}

    {% endfor %}

    </div>

{% endblock %}
